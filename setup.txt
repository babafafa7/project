> Build Jar file in projet directory
$ mvnw.cmd clean package -DskipTests

> Build angular web view in projet-web directory
$ ng build

> Dockerize API
$ docker-compose.exe up -d

>ALL IN ONE
$ cd projet && mvnw.cmd clean package -DskipTests && cd ../projet-web/ && ng build && cd .. && docker-compose.exe up -d

Mysql Docker :

> Download mysql and run it
$ docker run --name db-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=password123 -e MYSQL_DATABASE=dbproject -d mysql

> List container running
$ docker ps

> List ALL container 
$ docker container ls -a

> Start Stop container
$ docker start|stop container_id

> Bash container
$ docker exec -it container_id bash

> Connect mysql in container & show table user
$ mysql -uroot -ppassword123
$ use dbproject;
$ select * from user;


> Docker startup problem
$ bcdedit /set hypervisorlaunchtype auto 